apiVersion: v1
kind: Service
metadata:
  name: release-name-mysql-service
spec:
  selector:
    app: release-name-mysql
  ports:
      - protocol: TCP
        port: 3306
        targetPort: 3306
  type: ClusterIP